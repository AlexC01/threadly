import NotFoundPage from "@/components/NotFoundPage";

const notFound = () => {
	return (
		<NotFoundPage
			title="No results found"
			description="Thread not found, please try again with another search"
		/>
	);
};

export default notFound;
